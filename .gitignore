.venv/
venv/
__pycache__/
*.pyc
*.pyo
.env
*.zip

# Large third-party repos — clone separately, do NOT push to your repo
GRiT/
sam2/
third_party/recognize-anything/
depth-anything-v2-large/
detectron2/

# Model weights and checkpoints
*.pth
*.pt
*.safetensors
*.bin
*.ckpt
*.pkl

# Large output directories
output_scene/
output_scenes/
output/

# Sample bundle output_scenes3: keep PNG + JSON in git; omit dense arrays (regenerate locally)
output_scenes3/**/*.npz

# Test inputs (sample images OK; bulk datasets not)
# images/   ← uncomment if you don't want to push sample images

# NumPy depth arrays (large per-image files)
*.npy

# YOLO weights (downloaded by ultralytics automatically)
yolov8*.pt

# Scratch / test artifacts
dummy_test.png

# Root-level ad-hoc pytest scripts and local fixture tree (do not push)
/test/
/test_*.py

# Ad-hoc reviewer / validation scripts (not committed)
local_reviewer_tests/

# HuggingFace cache
.cache/

# OS / IDE
.DS_Store
*.swp
*.swo

# Markdown: do not ignore all *.md (committed docs live under docs/).
